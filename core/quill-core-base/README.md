# Quill Core Base Module
This module is the base module for Quill Core. It contains the common utilities, the base traits and structs for the Quill Core.

## Crate Features
- `python-binding`: Enable the Python binding to access the Manifest struct in Python. This feature is disabled by default.